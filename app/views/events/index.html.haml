%h1 Event Log
.table-responsive
  %table.table.table-striped.table-condensed.table-hover
    %thead
      %tr
        %th Event ID
        %th Time
        %th User
        %th Visit ID
        %th IP Address
        %th City
        %th State/Region
        %th Country
        %th Lat./Lon.
        %th Browser
        %th OS
        %th Device Type
        %th Landing Page
        %th User
        %th Actions
        %th Referrer
        %th User Agent
        %th Actions
    %tbody
      - @events.each do |event|
        %tr
          %td= event.id
          %td= event.time.in_time_zone("Pacific Time (US & Canada)")
          %td= event.user_id
          %td= event.visit.id
          %td= event.visit.ip
          %td= event.visit.city
          %td= event.visit.region
          %td= event.visit.country
          %td= "#{event.visit.latitude} / #{event.visit.longitude}"
          %td= event.visit.browser
          %td= event.visit.os
          %td= event.visit.device_type
          %td= event.visit.landing_page
          %td= event.user.name if event.user
          %td= event.properties
          %td= event.visit.referrer
          %td= event.visit.user_agent
          %td= link_to "Delete", event_path(event), method: :delete,              |
              data: { confirm: "Are you sure you want to delete this record?" }, |
              class: "btn btn-xs btn-danger"                                     |
